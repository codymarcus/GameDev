using UnityEngine;
using System.Collections;

public class autoRotate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//transform.Rotate(0, -40 + Random.value*80, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}